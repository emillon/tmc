  - add a non-verbose output
  - cut track
  - Cache: put the "FilePath -> IO ()" into the cOBject
  - cabalize
  - add a ToArg class for stuff that can be passed to sox
  - move alignTo / warpTo to prog
